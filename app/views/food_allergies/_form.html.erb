<%= form_for([@food_allergy.patient, @food_allergy]) do |f| %>
  <% if @food_allergy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_allergy.errors.count, "error") %> prohibited this food_allergy from being saved:</h2>

      <ul>
      <% @food_allergy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
